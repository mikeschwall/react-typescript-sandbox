import React, { useContext } from 'react';
import { CssBaseline } from '@mui/material';
import TodoList from './Todolist';

const App:React.FC = () => {
   
    return (
        <>
        <TodoList/>
        </>
    )
}

export default App;